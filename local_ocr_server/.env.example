# Local PaddleOCR Server Configuration
HOST=127.0.0.1
PORT=8001
OCR_LANG=hi
# Optional API Key for local access control. If empty, auth is disabled.
LOCAL_OCR_API_KEY=
# Maximum upload image size in megabytes
MAX_IMAGE_SIZE_MB=15
# Max dimension constraint to prevent OOM
MAX_IMAGE_DIMENSION=2000
